# Port for the server
PORT=1337

# Secret key for JWT (use a strong, unique key in production)
JWT_SECRET=your_jwt_secret_key_here

# Origin allowed for CORS (use * for all origins)
ORIGINS_ALLOWED=http://localhost:8100